v1.9.3 – Folder click + compact library + ultra-fine PDF finger scroll

- Fixed folder opening by mirroring the native list highlight after every rebuild.
- Handles currentSelectItemIndex robustly, including index 0 / protobuf omissions.
- Removed confusing page dots entirely.
- Piece count is now rendered as tiny bitmap text beside the Wetscher Optik logo, not as large G2 text.
- Removed folder counts from the selectable row labels to keep the list cleaner.
- Freed the whole lower display for the native list, so more choices are visible at once.
- PDF finger scrolling now moves exactly 1 SOURCE pixel per swipe, the smallest meaningful source-image step.
- Wetscher Optik logo remains in the library.
