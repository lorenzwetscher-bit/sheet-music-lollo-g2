v1.8.8 – Stability + instant library
- Fix: createStartUpPageContainer is never called twice after reconnect.
- Native G2 library restored for instant selection without image retransmission on each swipe.
- Immediate loading indicator before opening a saved score.
- Score images are prefetched and cached; BLE image sends remain serialized.
- Scroll master switch plus Time mode or Finger scroll mode.
- Single click in score returns to library; double click keeps the required system exit behavior.
- PDF inline worker and folder support retained.
