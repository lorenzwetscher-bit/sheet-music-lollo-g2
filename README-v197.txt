Sheet Music v1.9.7

Recovery build: library/menu path restored to the last known-good v1.9.3 implementation.
- Exact known-good library container geometry restored (288x36 logo placeholder, native list).
- On-screen piece count removed.
- Logo enlarged inside the known-good container without changing the container geometry.
- Phone folder open/closed state logic from later build retained.
- PDF/finger-scroll logic from later build retained.
- min_app_version corrected to 2.2.9 for SDK 0.0.14.

This build intentionally removes the later menu recovery/rebuild experiments that caused the app to remain on the startup page.
