Sheet Music for G2 v2.0.4
- Automatic German/English UI based on phone language (English when navigator language is English; German remains default otherwise).
- G2 library/start/status strings localized as well.
- Finger/timed prompter speed: deduplicated in-flight frame rendering, cache increased to 32 prepared frames, and deeper directional prefetch (up to 6 frames ahead).
- Prompter prefetch starts immediately when scrolling is enabled.
- Menu design/layout unchanged from v2.0.3.
