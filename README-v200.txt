v2.0.0 – Folder management + speed audit

- Phone folders can be renamed and deleted.
- Deleting a folder keeps all scores and moves them to “Ohne Ordner”.
- G2 menu design is unchanged from v1.9.9.
- Library metadata loads in parallel.
- Wetscher logo PNG is encoded once and reused after page rebuilds.
- Newly saved scores persist detected staff bounds so reopening skips staff detection.
- Existing page prefetch/cache, native G2 list scrolling, serialized image sends and inline PDF worker are retained.
