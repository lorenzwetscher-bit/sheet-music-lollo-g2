Sheet Music G2 v1.7.9

PDF import restored exactly to the known-good v1.6 implementation:
- same pdfjs-dist import
- same inline worker setup
- same getDocument({ data }) call
- same PDF file input and MIME-type routing

All v1.7.x folder/library/navigation features remain unchanged.
